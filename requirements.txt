# Runtime dependencies for the deployed app (app.py).
# Deliberately lean — matplotlib is NOT needed server-side; the browser renders
# charts as SVG. See requirements-dev.txt for the local CLI.
flask>=3.0
numpy>=1.26
scipy>=1.11
